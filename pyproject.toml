[project]
name = "open-meteo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "argparse>=1.4.0",
    "geopy>=2.4.1",
    "requests>=2.32.3",
]
